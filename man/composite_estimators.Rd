% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimator.R
\name{composite_estimators}
\alias{composite_estimators}
\alias{cqe}
\alias{cee}
\title{Composite quantile and expectile estimators}
\usage{
cqe(tau_cutoff = 0, weight = function(tau) 1)

cee(tau_cutoff = 0, weight = function(tau) 1)
}
\arguments{
\item{tau_cutoff}{Lower limit of the level range to fit over; a single
number in \verb{[0, 1)}. The estimator integrates the loss over levels from
\code{tau_cutoff} to 1, so a larger value concentrates the fit on the upper
tail. Defaults to \code{0} (the whole range).}

\item{weight}{A weight function applied across levels, taking a numeric
vector of levels in \verb{(0, 1)} and returning a numeric vector of weights.
Defaults to a constant weight of 1.}
}
\value{
An estimator object of class \code{"famish_estimator"} to be passed to
\code{\link[=fit_dst]{fit_dst()}}.
}
\description{
Configure a composite estimation method for use with \code{\link[=fit_dst]{fit_dst()}}. These
estimators fit a distribution family by matching its quantile function
(\code{cqe()}) or expectile function (\code{cee()}) to the data over a range of
levels, rather than at a single summary like the mean or the likelihood.
}
\details{
Composite quantile estimation (\code{cqe()}) chooses the family parameters
\code{theta} that minimise

\deqn{\frac{1}{n} \sum_{i=1}^n \int_{\tau_0}^1 w(\tau)\,
  \rho_\tau\!\left(y_i - Q(\tau; \theta)\right) d\tau,}

where \eqn{\rho_\tau} is the quantile (pinball) loss, \eqn{w} is the weight
function, \eqn{\tau_0} is the cutoff, and \eqn{Q(\cdot; \theta)} is the
family's quantile function. Composite expectile estimation (\code{cee()}) is
identical except the expectile loss \eqn{\eta_\tau(t) =
|\tau - \mathbf{1}(t < 0)|\, t^2} replaces \eqn{\rho_\tau}, and the family's
expectile function replaces its quantile function. Where matching quantiles
is a tail analogue of matching ranks, matching expectiles is a tail analogue
of the method of moments.

Pass the result to \code{\link[=fit_dst]{fit_dst()}} as the \code{method} argument. The strings
\code{"cqe"} and \code{"cee"} are shortcuts for \code{cqe()} and \code{cee()} with their
defaults.

Composite expectile estimation requires the family's expectile function,
which is only defined for distributions with a finite mean; it is currently
supported for continuous families.
}
\examples{
set.seed(1)
x <- rgamma(100, shape = 2, rate = 1)
# Composite quantile estimation over the whole range:
fit_dst("gamma", x, method = cqe())
# Concentrate the fit on the upper tail:
fit_dst("gamma", x, method = cqe(tau_cutoff = 0.8))
# Composite expectile estimation:
fit_dst("gamma", x, method = cee())
}
\seealso{
\code{\link[=fit_dst]{fit_dst()}}
}
